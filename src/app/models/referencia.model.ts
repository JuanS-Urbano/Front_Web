/**
 * DTO de Referencia.
 *
 * Corresponde a: com.grupo1.editorprocesos.dto.ReferenciaDTO
 */
export interface Referencia {
  id: number;
  tipo: string;
  referenciaId: number;
  // TODO: verificar campos adicionales del backend
}
