/**
 * DTO de Pool.
 *
 * Corresponde a: com.grupo1.editorprocesos.dto.PoolDTO
 */
export interface Pool {
  id: number;
  nombre: string;
  empresaId: number;
  // TODO: verificar campos adicionales del backend
}
